# Pick Hawaii Starter (Astro)

This is the basic Astro setup for your multilingual real estate + business listing site. To get started:

1. Run `npm install`
2. Run `npm run dev` to preview locally
3. Push to GitHub and connect to Cloudflare Pages